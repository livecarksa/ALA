# قواعد المستودع

- كود منصة «وصلة» يعيش حصرياً داخل `wasla/` — لا يُخلط أبداً مع أي كود خاص بـ live car.
- أي كود مستقبلي لـ live car يُبنى خارج `wasla/` ولا يستورد منها ولا تستورد منه.
- **الخطة الحاكمة للمشروع (نسخة بنكك) في `wasla/CLAUDE.md` — اقرأها أول كل جلسة والتزم بمراحلها وقراراتها.**
- المحرك (`wasla/packages/wasla_core`) سرّ تجاري: المستودع يبقى خاصاً ولا تُوثَّق خوارزمياته في أي ملف يخرج من الفريق.
