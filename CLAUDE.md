# قواعد المستودع

- كود منصة «وصلة» يعيش حصرياً داخل `wasla/` — لا يُخلط أبداً مع أي كود خاص بـ live car.
- أي كود مستقبلي لـ live car يُبنى خارج `wasla/` ولا يستورد منها ولا تستورد منه.
- تفاصيل معايير وصلة في `wasla/CLAUDE.md`.
