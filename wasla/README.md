# وصلة — نواة التحويلات الأوف لاين

> طبقة الدفع التي لا تسقط مع الشبكة — فوق المنظومة المصرفية السودانية

هذا المستودع يحتوي **النموذج الأولي لمحرك التوكنات الموقّعة** — الملكية الفكرية الجوهرية لمنصة وصلة (القسم 4 من دراسة الجدوى): تحويل مالي جهازاً لجهاز عند الانقطاع الكامل للاتصالات، يُسوّى نهائياً عبر القطاع المصرفي فور عودة الشبكة.

## الفكرة في سطرين

المرسل يوقّع توكناً بمفتاح Ed25519 داخل جهازه وينقله للمستلم عبر بلوتوث/QR/SMS بلا أي شبكة. كل توكن مرتبط بسابقه في **سلسلة توقيعات متتابعة**، فأي محاولة إنفاق مزدوج تُكشف حتمياً عند التسوية كتفرع في السلسلة.

## طبقات الدفاع الأربع ضد الإنفاق المزدوج

| الطبقة | الآلية | أين في الكود |
|---|---|---|
| السقف اليومي | يحدّ الخسارة القصوى لأي اختراق | `token_engine/wallet.py` + إعادة الفحص في `settlement/engine.py` |
| سلسلة التوقيعات | كل توكن يحمل hash سابقه — التفرع يُكشف عند التسوية | `token_engine/token.py` + `settlement/engine.py` |
| انتهاء الصلاحية | توكن غير مسوّى خلال 72 ساعة يسقط وتعود قيمته للمرسل | `token_engine/token.py` |
| البيئة الآمنة للمفاتيح | خارج نطاق النموذج الأولي — موثقة في المعمارية | `docs/architecture.md` |

## البنية

```
wasla/
├── token_engine/          # محرك التوكنات الموقّعة (الأصل الفكري الجوهري)
│   ├── crypto.py          #   مفاتيح Ed25519 وتسلسل قانوني للحمولة
│   ├── token.py           #   التوكن الموقّع وسلسلة التوقيعات
│   ├── wallet.py          #   المحفظة الأوف لاين: رصيد، سقف يومي، رأس السلسلة
│   └── errors.py
├── settlement/            # قضبان التسوية (تمثيل بيئة اختبار بنكك)
│   ├── engine.py          #   التحقق النهائي وكشف التفرع (داخل/عبر الدفعات) وتجميد المحتالين
│   ├── ledger.py          #   دفتر مزدوج القيد — ميزان مراجعة صفري دائماً
│   └── simulator.py       #   محاكي يوم انقطاع كامل: تحويلات + هجوم + تسوية
├── transport/             # طبقات النقل جهازاً لجهاز بلا شبكة
│   ├── codec.py           #   ترميز ثنائي مضغوط (188 بايت) — أساس كل القنوات
│   ├── sms.py             #   مسار SMS متعدد المقاطع للهواتف العادية
│   └── qr.py              #   حمولة QR للهواتف الذكية
├── gateway/               # بيئة اختبار بنكك (تمثيل اتفاقية الوصول التقني)
│   ├── service.py         #   المحرك فوق SQLite معمّرة + idempotency بمعرّف دفعة
│   ├── sandbox.py         #   خادم HTTP REST بمصادقة موقّعة
│   ├── auth.py            #   توقيع Ed25519 لكل طلب (إثبات حيازة)
│   └── client.py          #   عميل stdlib يوقّع تلقائياً
├── app/                   # التطبيق المبسط (يقوم مقام Flutter في النموذج)
│   ├── cli.py             #   انشئ/ارسل/استلم/سوّ/رصيد — عربي أولاً
│   └── demo.py            #   العرض الحي: تحويل أوف لاين كامل وتسويته
├── tests/                 # 62 اختباراً — كل اختبار أمني هجوم فعلي بتوكنات يدوية
│   ├── test_double_spend.py        # تفرع داخل/عبر الدفعات، replay، سلاسل مفبركة
│   ├── test_daily_cap.py           # السقف في المحفظة والمحرك
│   ├── test_expiry_and_tamper.py   # الصلاحية والتلاعب
│   ├── test_settlement.py          # المسار السعيد وسلامة الدفتر والمحاكي
│   ├── test_transport.py           # الترميز وSMS وQR مع التلف
│   ├── test_gateway.py             # HTTP، الديمومة عبر إعادة التشغيل، idempotency
│   ├── test_auth.py                # مصادقة الطلبات الموقّعة
│   ├── test_app.py                 # العرض الحي ودورة CLI
│   └── test_review_hardening.py    # تثبيت إصلاحات مجلس الخبراء
└── docs/architecture.md   # المعمارية ونموذج التهديد والحدود المعروفة
```

## التشغيل

المتطلبات: Python ‏3.11+ ومكتبة `cryptography` (و`pytest` للاختبارات).

```bash
cd wasla
pip install -r requirements.txt

# كل الاختبارات (70) — إنفاق مزدوج، سقوف، نقل، بوابة، مصادقة، تطبيق
python3 -m pytest tests/ -v

# العرض الحي للبوابة الثانية: تحويل أوف لاين كامل بين جهازين وتسويته
python3 -m app.demo

# محاكي يوم تسوية كامل بمخرجات عربية — حتمي عبر البذرة
python3 -m settlement.simulator --seed 7 --users 8

# تشغيل بيئة اختبار بنكك مستقلة (HTTP + SQLite معمّرة)
python3 -m gateway.sandbox --port 8980 --db wasla-sandbox.db
# ثم في صدفة أخرى: WASLA_GATEWAY=http://127.0.0.1:8980 python3 -m app.cli انشئ --اسم ali --ايداع 1500
```

## حدود النموذج الأولي

هذا إثبات للنواة الخوارزمية وطبقاتها (بوابة الدراسة الثانية: نموذج أولي عامل خلال 10 أسابيع)، وليس نظام إنتاج:

- المفاتيح في الذاكرة/الملف لا في البيئة الآمنة للجهاز (Secure Enclave / StrongBox) — الطبقة الرابعة.
- بوابة الاختبار تمثّل واجهات بنكك ولا تتصل بها فعلياً (طلباتها موقّعة لكنها محاكية).
- طبقات النقل ممثلة كبنى بيانات/نص (لا راديو BLE فعلي ولا بوابة SMS حقيقية).
- لا KYC ولا مراقبة أنماط مشبوهة بعد (القسم 7.2 من الدراسة).
- تضخيم السقف بالتأريخ الخلفي مقيَّد بنافذة العمر لا ملغى — راجع «حدود معروفة» في `docs/architecture.md`.

## قاعدة الفصل

كود وصلة كله داخل هذا المجلد (`wasla/`) ولا يُخلط بأي كود آخر في المستودع — راجع `CLAUDE.md`.
